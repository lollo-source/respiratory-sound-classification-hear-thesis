# Frozen-reference provenance

These values freeze the audited thesis state identified by manuscript SHA-256 `f89b6b8e…258cbd7`. They were cross-checked during assembly against the harmonised `final_metrics.csv` and `final_per_class_recall.csv`, OPERA `golden_v1` metrics, the adopted Challenge replay metrics/placements, both canonical bootstrap summaries/difference files, and protocol/event-capture records.

They are verification-only. Reproduction code does not import or read this directory. If a recomputation differs, the verifier fails and the discrepancy must be traced; outputs are never corrected to match this reference.

